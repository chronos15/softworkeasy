// Automatic FlutterFlow imports
import '/backend/schema/structs/index.dart';
import '/backend/schema/enums/enums.dart';
import '/actions/actions.dart' as action_blocks;
import '/flutter_flow/flutter_flow_theme.dart';
import '/flutter_flow/flutter_flow_util.dart';
import 'index.dart'; // Imports other custom actions
import '/flutter_flow/custom_functions.dart'; // Imports custom functions
import 'package:flutter/material.dart';
// Begin custom action code
// DO NOT REMOVE OR MODIFY THE CODE ABOVE!

/*import 'package:windows_single_instance/windows_single_instance.dart';
import 'dart:io';
import 'dart:ffi';
import 'package:win32/win32.dart';*/

Future<void> oneCallApplication() async {
/*  final args = <String>[];
  // Função de callback chamada quando uma segunda instância é detectada
  if (Platform.isWindows) {
    await WindowsSingleInstance.ensureSingleInstance(
      args,
      "custom_identifier",
      onSecondWindow: (args) {
        // Identifica a janela da aplicação original usando um título conhecido ou classe de janela
        final hWnd = FindWindow(nullptr, TEXT(''));

        // Traz a janela original para frente, se encontrada
        if (hWnd != 0) {
          SetForegroundWindow(hWnd);
        }
      },
    );
  }*/
}
